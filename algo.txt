algo
var 
ch:string,
long:integer:=0,
klm:integer:=0,
hrf:integer:=0,
i:integer;
begin

write('donner une chaine');
read(char);

for i from 1 to char.length step step do 

long:=long+1;


if(ch(i)=" "&ch(i)=".")
klm:=klm+1;
end if

if(ch(i)>="a" && ch(i)<="z" && ch(i)>="A" && ch(i)<="Z")
hrf:=hrf+1;
end if;


end for;
Write(long);
Write(klm);
Write(hrf);
end.